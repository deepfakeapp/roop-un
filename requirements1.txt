--extra-index-url https://download.pytorch.org/whl/cu118

# Core libraries
numpy==1.26.4
fastapi==0.115.2
pydantic==2.10.6
opencv-python-headless==4.9.0.80
onnx==1.16.0
psutil==5.9.6
tqdm==4.66.4

# Deep learning
torch==2.5.1+cu118; sys_platform != 'darwin'
torch==2.5.1; sys_platform == 'darwin'
torchvision==0.20.1+cu118; sys_platform != 'darwin'
torchvision==0.20.1; sys_platform == 'darwin'

# ONNX runtimes
onnxruntime==1.22.0; sys_platform == 'darwin' and platform_machine != 'arm64'
onnxruntime-silicon==1.16.3; sys_platform == 'darwin' and platform_machine == 'arm64'
onnxruntime-gpu==1.22.0; sys_platform != 'darwin'

# InsightFace & image processing
insightface==0.7.3
albucore==0.0.16

# Utilities
ftfy
regex
pyvirtualcam

# Gradio (Latest compatible ≥5.1)
gradio==5.25.0
gradio-client==1.3.0
